import styled from "styled-components";

const IconArrowStyled = styled.a`
  border-radius: 50%;
  background: rgba(0, 0, 0, 0.7);
  width: 32px;
  height: 32px;
  display: flex;
  justify-content: center;
  align-items: center;
  cursor: pointer;
`;

export { IconArrowStyled };
